# 剩余执行计划（T11、T12 + 冒烟测试）

已完成并验证通过（`tests/` 29 项全绿）：
- T1 市场回环 agent8→agent1 ✅
- T2 立项被否换书重试（agent1 排除已试书目、"failed" 跳过、"scout_excluded" 记账）✅
- T3 放开 Agnes 硬阻塞，纯文本阶段可跑 ✅
- T4 Agent7 情绪段切片 + 投流元数据（GrowthAsset 增 headline/description/tags；新增 GrowthMeta）✅
- T5 TTS 配音/混音管线（`src/tts.py`，可配置、缺 TTS 降级）✅
- T6 字幕大字报烧录（`src/subtitles.py` + Agent6 接入）✅
- T7 逐镜归一化后拼接（`concat_videos` 增加 `_normalize_shots`）✅
- T8 角色一致性（`CharacterSheet` + `src/characters.py`，Agent3 抽取、Agent4/5 注入）✅
- T9 结构化日志（`run_logs` 表 + main 记录）+ Agnes 成本护栏（`src/cost_tracker.py`，Agent5 集成）✅
- T10 修 render_failed 跨 run 空转（`_render_failed_retryable`，终态不再重入 Agent5）✅

## T11：依赖 lockfile + CI
- 生成 `requirements.lock`（`pip freeze` 导出于服务器端完整环境；本地用 `-r requirements.txt` 兼容）。
- 新增 `.github/workflows/ci.yml`：装依赖 → `pytest tests/ -q`；媒体/ffmpeg 相关均走 mock，无 ffmpeg 也能跑绿。
- 说明：本地无 ffmpeg，所有媒体调用已设计为「有则真跑、无则降级/mock」。

## T12：补测试并跑绿
新增 `tests/test_workflow_enhancements.py`（及必要的独立测试文件），覆盖：
1. 图路由：立项被否换书（attempts<max → agent1；达上限 → END）
2. 图路由：市场回环（cycle<max 且成片完成 → agent1；否则 END）
3. Agent1 换书排除已试书目（scrape_biquge_novel 的 exclude_titles + scout_excluded 记账）
4. Agent7 情绪段识别与投流元数据（hook/climax + headline/tags）
5. TTS 降级（无 TTS URL → 返回 None，不破坏主链路）
6. 字幕 ASS 生成（build_ass_track 含样式/时间戳/空对白跳过）
7. 成本护栏（budget_exhausted 熔断、write_report 生成 CSV）
8. render_failed 终态判定（_render_failed_retryable：无分镜/达重写上限 → 不重入）
9. 角色抽取与提示注入（extract_characters 净化 + build_agnes_prompt 一致性块）
10. 状态模型：EpisodeState 新增字段（audio_track/subtitle_track/growth_meta/characters）round-trip

最后：
- 本地 `pytest tests/ -q` 全绿冒烟验证。
- 用 mock Agnes + fake 媒体做一条端到端冒烟：`agent1→8→回环`（设 MAX_CYCLES=2）确认整图可跑、节点流转与 run_logs 落库。
- 服务器端（具备完整环境）实跑项在 README/说明中标注。

## 交付说明
- 所有改动均在 `code/` 下，未触碰真实密钥（.env 未改）。
- ffmpeg/Agnes 真实调用在本地无法实跑，均以 mock 覆盖验证逻辑，服务器端实跑验证即可。